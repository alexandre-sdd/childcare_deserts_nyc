# TODO

## Audit Follow-Up

- [x] Record instructor clarification for the Part 1 expansion cap: both `x_f <= min(1.2 n_f, 500 - n_f)` and `x_f <= min(1.2 n_f, 500)` are acceptable. Future audit notes should not treat Option 2 as a modeling error.
- [ ] Keep the remaining technical issues separate from that clarification: Part 1 still uses a non-literal expansion-cost implementation, and the ideal demand table still creates artificial requirements in zero-child ZIP codes.
